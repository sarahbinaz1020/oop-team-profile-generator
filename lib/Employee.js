// build out classes - this is parent
class Employee {
  constructor(name, id, email) {
    this.name = name;
    this.id = id;
    this.email = email;
  }
}

// will need .getName()
// will need .getId()
// will need .getEmail()
// will need .getRole() - returns "Employee"
